# Shared packages

Put code here only when at least two apps share a real responsibility.

Likely future packages include `ui`, `media`, and `config`. Until those seams are
real, keeping feature code inside its owning app makes the repository easier to
change.
